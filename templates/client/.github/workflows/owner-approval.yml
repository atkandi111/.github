name: Owner approval

on:
  pull_request_review:
    types: [submitted]

permissions:
  contents: read
  statuses: write

jobs:
  approval:
    uses: YOUR_OWNER/.github/.github/workflows/owner-approval.yml@main
